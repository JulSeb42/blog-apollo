/*=============================================== CommentLine types ===============================================*/

import { CommentType } from "../../../types"

export interface CommentLineProps {
    comment: CommentType
}
